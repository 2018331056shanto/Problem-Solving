#inlcude<bits/stdc++.h>
using namespace std;
struct node
{

    int val;
    node *left,*right;
};
node *newnode(int val)
{

    node *temp;
    temp=new node;
    temp->left=tmep->right=NULL;
    return temp;
}
node *sortedtree(int a[],node *root,)


}
int main()
{
    node *root;
    int a[1000];
    int n,i,j;
    cin>>n;
    for(i=0;i<n;i++)
    {

        cin>>a[i];
    }
   root=binrary_s_tree(a,root,0,n);
}
