#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>>t;
    while(t--)
    {
        int n,x,y;
        cin>>n>>x>>y;
        for(int i=1;i<(x-y);i++)
        {
            if((x-y)%i==0&&())
        }
    }
}