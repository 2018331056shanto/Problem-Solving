
		
		cout<<"already had root";
		tree[0]=a;
	 
	cin>>n;
	else
	if(tree[0]!=NULL)
	if(tree[data]==NULL)
	int c,int tree[1000];
	root(n ,tree);
#include<bits/stdc++.h>
int main()
using namespace std;
void left_child(int tree[],int data)
void root(int a,int tree[])
